# Continue Logging Rules

1. Never run git commands or terminal commands.
2. Inspect changed files (`@diff`) or recent commits (`@commit`).
3. Write or update `studyLog.md` and `challengeLog.md` directly.
4. Stop immediately after modifying the markdown files.